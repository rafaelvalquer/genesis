import { enemyBehavior } from "../enemyBehavior.js";
export const carapacaNereidaBehavior = enemyBehavior({ createState: (session, queued, config) => ({ nereidaState: "spawnEmerge", nereidaStateStartedAt: session.elapsed, nereidaStateEndsAt: session.elapsed + config.spawnDurationMs, nereidaAttackApplied: false, nereidaAttackTargetId: null, nereidaMovementDistance: 0, lastHitAt: -Infinity }), update: (runtime, enemy, config, dt, events) => (runtime.updateCarapacaNereida(enemy, config, dt, events), true), receiveDamage: (runtime, enemy) => { enemy.lastHitAt = runtime.elapsed; } });
